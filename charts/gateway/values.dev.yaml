gateway:
  replicaCount: 1
  image:
    repository: haeseung/mallowlink-pay-gateway
    tag: develop-9f70f43ed0
    pullPolicy: IfNotPresent
  configmap:
    enabled: true
    values:
      SPRING_PROFILES_ACTIVE: dev
      config_host: ml-config-rollout-active-svc.ml-dev # configmap
  ingress:
    enabled: false
    className: nginx
  envFrom:
    - configMapRef:
        name: pay-test-gateway-cm
    - secretRef:
        name: config-key-scr

  rollout:
    strategy:
      blueGreen:
        autoPromotionEnabled: true
  secrets:
    enabled: false
  otel:
    exporterEndpoint: "http://monitoring-opentelemetry-collector.logging:4317"


